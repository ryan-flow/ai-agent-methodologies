# AI Agent 调试方法论

当 AI 编程助手遇到问题时，按以下方法论排查。

## 方法论

- **约束优先排查**：先验证 API / 平台是否支持目标功能，再动手改代码。详见 `skills/constraint-first-debugging/`

## 使用方式

直接在对话中提及方法论名称，AI 助手会自动加载对应的排查流程。

## 添加新方法论

在 `skills/` 目录下新建文件夹，写入 `SKILL.md` 即可。
